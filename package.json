{
  "name": "popskullys-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:hosted": "graph deploy SoulSwapFinance/nftkey --product hosted-service",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ SoulSwapFinance/nftkey",
    "create-local": "graph create --node http://localhost:8020/ SoulSwapFinance/nftkey",
    "remove-local": "graph remove --node http://localhost:8020/ SoulSwapFinance/nftkey",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 SoulSwapFinance/nftkey"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
